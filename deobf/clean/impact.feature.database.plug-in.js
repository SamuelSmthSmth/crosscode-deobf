/**
 * impact.feature.database.plug-in
 * ===============================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.database.plug-in")`.
 *
 * Subsystem entry point for the database (pulls in the database module).
 */
ig.module("impact.feature.database.plug-in")
    .requires("impact.feature.database.database")
    .defines(function () {});
ig.baked = !0;
