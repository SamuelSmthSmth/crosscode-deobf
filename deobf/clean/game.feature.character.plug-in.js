/**
 * game.feature.character.plug-in
 * ==============================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("game.feature.character.plug-in")`.
 *
 * Character subsystem entry point. Pulls in character data loading and
 * character template registration.
 */
ig.module("game.feature.character.plug-in").requires(
    "game.feature.character.character",
    "game.feature.character.char-templates"
).defines(function () {});
ig.baked = !0;