/**
 * game.beta
 * =========
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("game.beta")`.
 *
 * Beta-feature aggregator: pulls in the beta plugin and the language-edit
 * plugin. No code of its own.
 */
ig.module("game.beta").requires("game.feature.beta.plug-in", "impact.feature.lang-edit.plug-in").defines(function () {});
ig.baked = !0;
