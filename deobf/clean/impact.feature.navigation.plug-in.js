/**
 * impact.feature.navigation.plug-in
 * =================================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.navigation.plug-in")`.
 *
 * Subsystem entry point: pulls in the nav-map graph, the navigation
 * add-on/entity, and the navigation action steps.
 */
ig.module("impact.feature.navigation.plug-in")
    .requires("impact.feature.navigation.nav-map", "impact.feature.navigation.navigation", "impact.feature.navigation.navigation-steps")
    .defines(function () {});
ig.baked = !0;
