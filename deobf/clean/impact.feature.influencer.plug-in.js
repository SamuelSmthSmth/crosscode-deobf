/**
 * impact.feature.influencer.plug-in
 * =================================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.influencer.plug-in")`.
 *
 * Subsystem entry point: pulls in the influencer system and its action steps.
 */
ig.module("impact.feature.influencer.plug-in")
    .requires("impact.feature.influencer.influencer", "impact.feature.influencer.influencer-steps")
    .defines(function () {});
ig.baked = !0;
