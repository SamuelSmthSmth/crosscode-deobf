/**
 * impact.feature.slow-motion.plug-in
 * ==================================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.slow-motion.plug-in")`.
 *
 * Subsystem entry point for slow motion and its steps.
 */
ig.module("impact.feature.slow-motion.plug-in")
    .requires("impact.feature.slow-motion.slow-motion", "impact.feature.slow-motion.slow-motion-steps")
    .defines(function () {});
ig.baked = !0;
