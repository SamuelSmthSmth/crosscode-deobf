/**
 * impact.feature.map-image.plug-in
 * ================================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.map-image.plug-in")`.
 *
 * Subsystem entry point for map images and their steps.
 */
ig.module("impact.feature.map-image.plug-in")
    .requires("impact.feature.map-image.map-image", "impact.feature.map-image.map-image-steps")
    .defines(function () {});
ig.baked = !0;
