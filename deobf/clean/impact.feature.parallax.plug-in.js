/**
 * impact.feature.parallax.plug-in
 * ===============================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.parallax.plug-in")`.
 *
 * The plug-in entry point for the parallax subsystem.
 * Pulls in the parallax loader/gui and its show-step via `requires`.
 */
ig.module("impact.feature.parallax.plug-in")
    .requires("impact.feature.parallax.parallax", "impact.feature.parallax.parallax-steps")
    .defines(function () {});
