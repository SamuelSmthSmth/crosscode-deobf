/**
 * impact.feature.rumble.plug-in
 * =============================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.rumble.plug-in")`.
 *
 * Subsystem entry point for the screen rumble system.
 */
ig.module("impact.feature.rumble.plug-in")
    .requires("impact.feature.rumble.rumble", "impact.feature.rumble.rumble-steps")
    .defines(function () {});
ig.baked = !0;
