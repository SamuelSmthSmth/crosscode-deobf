/**
 * game.constants
 * ==============
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("game.constants")`.
 *
 * Global `window.Constants` for ball physics (used by puzzle elements).
 */
ig.module("game.constants").defines(function () {
    window.Constants = window.Constants = {
        BALL_HEIGHT: 12,
        BALL_SIZE: 8,
        BALL_Z_HEIGHT: 8
    }
});
ig.baked = !0;
