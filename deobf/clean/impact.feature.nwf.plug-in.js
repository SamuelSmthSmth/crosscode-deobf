/**
 * impact.feature.nwf.plug-in
 * ==========================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.nwf.plug-in")`.
 *
 * Subsystem entry point for the Wii U (NWF) error handling.
 */
ig.module("impact.feature.nwf.plug-in")
    .requires("impact.feature.nwf.nwf-errors")
    .defines(function () {});
ig.baked = !0;
