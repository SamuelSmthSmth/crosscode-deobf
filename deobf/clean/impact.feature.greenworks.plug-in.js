/**
 * impact.feature.greenworks.plug-in
 * =================================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.greenworks.plug-in")`.
 *
 * The plug-in entry point for the Steam (Greenworks) integration.
 * Its only responsibility is to pull in the greenworks game add-on via
 * `requires`.
 */
ig.module("impact.feature.greenworks.plug-in")
    .requires("impact.feature.greenworks.greenworks")
    .defines(function () {});
