/**
 * game.feature.ar.plug-in
 * =======================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("game.feature.ar.plug-in")`.
 *
 * AR (augmented reality) subsystem entry point — pulls in the AR box GUI
 * and AR event/action steps.
 */
ig.module("game.feature.ar.plug-in").requires(
    "game.feature.ar.gui.ar-box",
    "game.feature.ar.ar-steps"
).defines(function () {});
ig.baked = !0;