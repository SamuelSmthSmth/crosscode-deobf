/**
 * impact.feature.terrain.plug-in
 * ==============================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("impact.feature.terrain.plug-in")`.
 *
 * The plug-in entry point for the terrain subsystem.
 * Its only responsibility is to pull in the terrain module via `requires`.
 */
ig.module("impact.feature.terrain.plug-in")
    .requires("impact.feature.terrain.terrain")
    .defines(function () {});
