/**
 * game.feature.beta.plug-in
 * =========================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("game.feature.beta.plug-in")`.
 *
 * Beta-feature plug-in entry point. Pulls in beta debug controls.
 */
ig.module("game.feature.beta.plug-in").requires(
    "game.feature.beta.beta-controls"
).defines(function () {});
ig.baked = !0;