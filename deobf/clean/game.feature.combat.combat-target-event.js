/**
 * game.feature.combat.combat-target-event
 * ========================================
 * Deobfuscated reference — documentation only, not executed.
 * Source module: `assets/js/game.compiled.js` → `ig.module("game.feature.combat.combat-target-event")`.
 *
 * Declares the `sc.COMBAT_ENEMY_EVENT` table that other combat modules
 * populate with enemy target-event keys. Empty here; filled in later.
 */
ig.module("game.feature.combat.combat-target-event").defines(function () {
    sc.COMBAT_ENEMY_EVENT = {}
});
ig.baked = !0;
